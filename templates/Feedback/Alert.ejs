import * as React from 'react';
import Alert from '@mui/material/Alert';
import AlertTitle from '@mui/material/AlertTitle';

const CustomAlert = ({ 
    alertTitle,
    alertBody, 
    onClose, 
    color='success', 
    severity='success', 
    variant='contained' 
}) => {
    return (
        <Alert 
            severity={severity} 
            color={color} 
            variant={variant} 
            onClose={onClose}
        >
                {alertTtitle && <AlertTitle>{alertTitle}</AlertTitle>}
                {alertBody}
        </Alert>
    )
};

export default CustomAlert;